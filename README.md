# Fun With Codex

Tiny playground built with Codex.

## Run

Open `index.html` in a browser.

## Commits

This repo is configured to always add a `Co-authored-by: codex <noreply@openai.com>` trailer on commits.
If you want a different co-author identity, edit `.githooks/prepare-commit-msg`.
